<Alloy>
  <View class="w-80 h-52 rounded-10 ios:ml-3">
    <ImageView image="{image}" class="bg-cover wh-full touch-enabled-false" />

    <Widget src="info" favorite="{favorite}" text="{text}" stars="{stars}" />
  </View>
</Alloy>
